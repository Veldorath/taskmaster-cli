# taskmaster-cli.
TaskMaster CLI, una pequeña herramienta de línea de comandos que gestiona una lista de tareas en un archivo JSON. El código es intencional y simple: lo importante es el flujo de trabajo, no la lógica del programa.
